generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

/// TODO: Add models for subjects, upload_sessions, uploaded_items in subsequent steps.
